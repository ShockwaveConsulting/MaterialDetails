--author: Sergio Guerrero
PROCEDURE "SP_VENDOR_CRT_UPD"( IN_VENDOR TABLE(VENDORUID NVARCHAR(50), NAME NVARCHAR(50), CATEGORY NVARCHAR(50)) )
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   --DEFAULT SCHEMA <default_schema_name>
   -- READS SQL DATA 
   AS
BEGIN
   
    -- for the reader, add logic to update        

    INSERT INTO "COM_SHOCKWAVECONSULTING_VENDORS" (VENDORUID, NAME, CATEGORY)
    SELECT NEWUID(), NAME, CATEGORY
    FROM :IN_VENDOR;


END